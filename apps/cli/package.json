{
  "name": "@launcheleven/srgical",
  "version": "0.0.0",
  "description": "A browser-first local workspace for AI-assisted planning and delivery.",
  "files": [
    "dist",
    "README.md",
    "CHANGELOG.md",
    "postinstall.js",
    "LICENSE"
  ],
  "bin": {
    "srgical": "dist/index.js"
  },
  "bundleDependencies": [
    "@srgical/agent-runtime",
    "@srgical/connector-registry",
    "@srgical/skill-registry",
    "@srgical/studio-core",
    "@srgical/studio-shared"
  ],
  "scripts": {
    "prebuild": "npm --prefix ../.. run build -w @srgical/studio-shared && npm --prefix ../.. run build -w @srgical/agent-runtime && npm --prefix ../.. run build -w @srgical/skill-registry && npm --prefix ../.. run build -w @srgical/connector-registry && npm --prefix ../.. run build -w @srgical/studio-core && npm --prefix ../.. run build -w @srgical/studio-web",
    "build": "node scripts/clean-dist.mjs && tsc -p tsconfig.json && node scripts/sync-web-dist.mjs",
    "dev": "tsx src/index.ts",
    "start": "node dist/index.js",
    "postinstall": "node postinstall.js",
    "pretest": "npm --prefix ../.. run build -w @srgical/studio-shared && npm --prefix ../.. run build -w @srgical/agent-runtime && npm --prefix ../.. run build -w @srgical/skill-registry && npm --prefix ../.. run build -w @srgical/connector-registry && npm --prefix ../.. run build -w @srgical/studio-core",
    "test": "tsx --test test/**/*.test.ts",
    "pretest:coverage": "npm --prefix ../.. run build -w @srgical/studio-shared && npm --prefix ../.. run build -w @srgical/agent-runtime && npm --prefix ../.. run build -w @srgical/skill-registry && npm --prefix ../.. run build -w @srgical/connector-registry && npm --prefix ../.. run build -w @srgical/studio-core",
    "test:coverage": "tsx --test --experimental-test-coverage test/**/*.test.ts"
  },
  "engines": {
    "node": ">=20"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/LaunchEleven/Srgical.git"
  },
  "homepage": "https://github.com/LaunchEleven/Srgical#readme",
  "bugs": {
    "url": "https://github.com/LaunchEleven/Srgical/issues"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  },
  "keywords": [
    "ai",
    "browser",
    "codex",
    "agent",
    "orchestration",
    "planning"
  ],
  "license": "MIT",
  "dependencies": {
    "@srgical/agent-runtime": "0.0.0",
    "@srgical/connector-registry": "0.0.0",
    "@srgical/skill-registry": "0.0.0",
    "@srgical/studio-core": "0.0.0",
    "@srgical/studio-shared": "0.0.0",
    "commander": "^14.0.3"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  }
}
